<!DOCTYPE html>
<html lang="en">
<%- include("./partials/head.ejs") %>
<body>
    <nav>
        <a href="/">Home Page</a>
        <a href="/about">About Page</a>

    </nav>
         <div>
            This is getting serious
            <%  if (blogs.length > 0) { %>

                <% blogs.forEach(blog => { %>
                        <h4><%= blog.title %></h4>
                        <p><%= blog.snippet %></p>
              <%  }) %>

            <% } %>
            </div>
</body>
</html>